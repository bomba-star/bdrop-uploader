#!/usr/bin/env python3
"""Standalone-Entry-Point fuer bdrop-encode (ohne pip-Installation nutzbar).

Aufruf direkt aus dem engine/-Ordner:
    ./bdrop-encode doctor
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from bdrop_encode.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
