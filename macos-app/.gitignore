# Xcode / SwiftPM Build-Artefakte
.build/
DerivedData/
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate

# Von XcodeGen generiert (aus project.yml). Nicht einchecken.
BDropUploader.xcodeproj/

# Gebuendelte ffmpeg-Binaries (gross, GPL, nicht ins Repo)
Resources/ffmpeg/ffmpeg
Resources/ffmpeg/ffprobe

# macOS
.DS_Store

# Signierte/notarisierte Artefakte
*.dmg
*.app
*.zip
